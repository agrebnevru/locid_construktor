program ICQCollector;

uses
  Forms,
  Main in 'Main.pas' {MainForm},
  Settings in 'Settings.pas' {FormSettings},
  Creator in 'Creator.pas' {FormCreator},
  LocID in 'LocID.pas' {FormLocID},
  Opening in 'Opening.pas' {FormDialog};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TMainForm, MainForm);
  Application.CreateForm(TFormSettings, FormSettings);
  Application.CreateForm(TFormCreator, FormCreator);
  Application.CreateForm(TFormLocID, FormLocID);
  Application.CreateForm(TFormCreator, FormCreator);
  Application.CreateForm(TFormDialog, FormDialog);
  Application.Run;
end.
